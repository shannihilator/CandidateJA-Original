<h1> SalesLoft: Level 2 </h1>

    <div>
        <table>
            <tr>
                <th>Character</th>
                <th>Count</th>
            </tr>
        <% @frequency.each do |k, v| %>
            <tr>
                <th><%= k %></th>
                <th><%= v %></th>
            </tr>
        <% end %>
        </table>
    </div>

<%= button_to  "Back to Home", root_path, method: :get %>